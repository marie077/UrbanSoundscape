1. install p5 serial controller here: https://github.com/p5-serial/p5.serialserver
2. run the p5 code either in a p5 web editor or locally in a browser
3. open arduino, compile and upload code if not already
4. Close Serial Monitor in arduino
5. open the p5 serial controller and open ports and check console log box
6. check if the p5 browser reacts to the sensor